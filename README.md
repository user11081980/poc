# Commands:
* python -m venv .venv
* .venv\Scripts\activate.bat
* pip install playwright requests pypdf
* python -m pip install azure-storage-blob
* playwright install chromium
* python scrape.py > scrape_logs.txt 2>&1